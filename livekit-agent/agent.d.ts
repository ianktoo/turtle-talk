import { voice } from '@livekit/agents';
/** Shelly — voice agent for children (aged 4–10). Used by the LiveKit pipeline. */
export declare class ShellyAgent extends voice.Agent {
    constructor(options?: {
        childName?: string;
        topics?: string[];
    });
}
//# sourceMappingURL=agent.d.ts.map