'use client';

export default function TagLine() {
  return (
    <p
      style={{
        margin: 0,
        fontSize: 'clamp(0.875rem, 2.5vw, 1rem)',
        fontWeight: 600,
        color: 'var(--v2-primary)',
        textAlign: 'center',
      }}
    >
      Talk to shelly
    </p>
  );
}
