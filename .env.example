# =============================================================
# Turtle Talk — environment variables
# Copy this file to .env.local and fill in the values.
# Lines prefixed with NEXT_PUBLIC_ are exposed to the browser.
# =============================================================

# Public URL of the deployed app (used for OG metadata and sitemap)
NEXT_PUBLIC_APP_URL=https://turtletalk.io

# -------------------------------------------------------------
# API Keys
# -------------------------------------------------------------

# OpenAI — STT (Whisper) and optional chat (gpt-4o-mini)
OPENAI_API_KEY=sk-...

# Anthropic — chat (claude-haiku-4-5, default provider)
ANTHROPIC_API_KEY=sk-ant-...

# ElevenLabs — TTS (Sarah voice, default TTS provider)
ELEVENLABS_API_KEY=sk_...

# Google Gemini — alternative STT and/or TTS provider
GEMINI_API_KEY=AIza...

# -------------------------------------------------------------
# Speech provider selection
# -------------------------------------------------------------

# Chat provider: "anthropic" (default) | "openai"
SPEECH_CHAT_PROVIDER=anthropic

# STT provider: "openai" (default) | "gemini"
SPEECH_STT_PROVIDER=openai

# TTS provider: "elevenlabs" (default) | "gemini"
SPEECH_TTS_PROVIDER=elevenlabs

# Voice session provider (browser): "native" (default) | "vapi"
NEXT_PUBLIC_VOICE_PROVIDER=native

# -------------------------------------------------------------
# Speech model / config overrides (optional)
# -------------------------------------------------------------

# OpenAI
# SPEECH_STT_MODEL=gpt-4o-mini-transcribe
# SPEECH_ANTHROPIC_MODEL=claude-haiku-4-5-20251001
# SPEECH_OPENAI_MODEL=gpt-4o-mini
# SPEECH_CHAT_MAX_TOKENS=512

# ElevenLabs
# ELEVENLABS_VOICE_ID=EXAVITQu4vr4xnSDxMaL   # default: Sarah
# ELEVENLABS_MODEL=eleven_turbo_v2_5           # default: turbo v2.5

# Gemini
SPEECH_GEMINI_STT_MODEL=gemini-3-flash-preview
SPEECH_GEMINI_TTS_MODEL=gemini-3-flash-preview-tts
# SPEECH_GEMINI_TTS_VOICE=Aoede

# -------------------------------------------------------------
# Vapi (only needed when NEXT_PUBLIC_VOICE_PROVIDER=vapi)
# -------------------------------------------------------------

# NEXT_PUBLIC_VAPI_PUBLIC_KEY=
# NEXT_PUBLIC_VAPI_ASSISTANT_ID=985d923d-6efc-43ef-b9a9-4b935c954a9c
# NEXT_PUBLIC_CUSTOM_LLM_URL=   # leave empty in prod; set to ngrok URL for local dev
# NEXT_PUBLIC_ELEVENLABS_VOICE_ID=EXAVITQu4vr4xnSDxMaL

# -------------------------------------------------------------
# Database
# -------------------------------------------------------------

# Provider: "localStorage" (default, no setup) | "supabase" | "convex"
NEXT_PUBLIC_DB_PROVIDER=localStorage

# Supabase — required when NEXT_PUBLIC_DB_PROVIDER=supabase
# NEXT_PUBLIC_SUPABASE_URL=https://<project>.supabase.co
# NEXT_PUBLIC_SUPABASE_ANON_KEY=eyJ...

# Convex — required when NEXT_PUBLIC_DB_PROVIDER=convex
# Run 'npx convex dev' to generate convex/_generated/ before use
# NEXT_PUBLIC_CONVEX_URL=https://<deployment>.convex.cloud

# -------------------------------------------------------------
# SMS
# -------------------------------------------------------------

# Provider: "plivo" (default)
SMS_PROVIDER=plivo

# Plivo credentials (server-side only)
PLIVO_AUTH_ID=
PLIVO_AUTH_TOKEN=
PLIVO_FROM_NUMBER=+1...

# -------------------------------------------------------------
# LangSmith — optional tracing
# -------------------------------------------------------------

# LANGCHAIN_TRACING_V2=true
# LANGCHAIN_API_KEY=
# LANGCHAIN_PROJECT=turtle-talk
